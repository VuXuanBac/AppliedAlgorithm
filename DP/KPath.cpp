/// Cho đồ thị có trọng số không có chu trình.
/// Tìm K-path trên đồ thị sao cho đường đi ngắn nhất.
#include <bits/stdc++.h>
using namespace std;
int N, K, M;
int main()
{
    // ios_base::sync_with_stdio(false); 
    // cin.tie(NULL);
    cin >> N >> K >> M;
    vector<vector<int> > paths(N + 1, vector<int>(N + 1));
    vector<vector<int> > edges()
    for(int i = 0; i < M; ++i){

    }
    return 0;
}
